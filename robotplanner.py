def computePath():

def robotplanner(envmap, robotpos, targetpos):
  # conditions that robot must obey
  # cannot step onto an obstacle
  # only one step at a time
  # cannot move beyond boundaries
  # may move farther to target
  # as part of some strategy

  newrobotpos = robotpos
  return newrobotpos
  